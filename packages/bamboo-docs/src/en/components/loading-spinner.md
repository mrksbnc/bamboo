---
title: Loading Spinner
description: Indicate pending work with a rotating spinner.
category: feedback
tags: [loading, spinner, feedback]
---

<script setup lang="ts">
import { BoLoadingSpinner } from '@mrksbnc/bamboo-vue';

const basicExample = `<bo-loading-spinner />`;
const textExample = `<bo-loading-spinner loader-text="Loading..." text-position="after" />`;
const sizesExample = `<div class="flex items-center gap-3">
  <bo-loading-spinner size="sm" />
  <bo-loading-spinner size="default" />
  <bo-loading-spinner size="lg" />
</div>`;
</script>

# Loading Spinner

Use `bo-loading-spinner` for a compact rotating pending indicator.

## Usage

<ExampleFrame :code="basicExample">
  <bo-loading-spinner />
</ExampleFrame>

## Text and sizes

<ExampleFrame :code="textExample">
  <bo-loading-spinner loader-text="Loading..." text-position="after" />
</ExampleFrame>

<ExampleFrame :code="sizesExample">
  <div class="flex items-center gap-3">
    <bo-loading-spinner size="sm" />
    <bo-loading-spinner size="default" />
    <bo-loading-spinner size="lg" />
  </div>
</ExampleFrame>

## API

| Prop                                           | Type                                                                            | Default                             | Description                    |
| ---------------------------------------------- | ------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------ |
| `id` / `dataTestId`                            | `string`                                                                        | autogenerated                       | Root attributes.               |
| `size`                                         | `xs \| sm \| default \| lg \| xl`                                               | `default`                           | Spinner size.                  |
| `variant`                                      | `primary \| secondary \| success \| warning \| destructive \| white \| current` | `primary`                           | Color variant.                 |
| `loaderText`                                   | `string`                                                                        | -                                   | Text shown with the indicator. |
| `textPosition`                                 | `top \| bottom \| before \| after`                                              | `after`                             | Text position.                 |
| `customColor`                                  | `string`                                                                        | -                                   | CSS color override.            |
| `role` / `ariaLive` / `ariaLabel` / `ariaBusy` | native accessibility types                                                      | `status` / `polite` / `Loading` / - | Live-region semantics.         |
