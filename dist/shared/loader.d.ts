export declare enum BoLoaderTextPosition {
    top = "top",
    bottom = "bottom",
    before = "before",
    after = "after"
}
export declare enum BoLoaderType {
    spinner = "spinner",
    ring = "ring"
}
