import { Canvas, Meta, Controls } from '@storybook/blocks';
import * as BoTextStories from './bo_text.stories';

<Meta
	title="Components/bo-text"
	component={BoTextStories.BoText}
/>

# bo-text

A label is a small piece of text that is used to display information.

## Basic usage

```html
<bo-text :text="Hello World!" />
```

## Props

- `text` - The text to be displayed (required)
  - default: `''`
- `size` - The size of the text (optional)
  - default: `BoFontSize.default`
- `weight` - The weight of the text (optional)
  - default: `BoFontWeight.regular`

## Examples

<Canvas of={BoTextStories.Example} />
<Controls of={BoTextStories.Example} />

## Sizes

The `size` prop can be used to change the size of the text. The `size` prop should be a member of the `BoFontSize` enum.

- `extra-small`
- `small`
- `default`
- `body`
- `title`
- `h6`
- `h5`
- `h4`
- `h3`
- `h2`
- `h1`

<Canvas of={BoTextStories.Sizes} />

## Weights

The `weight` prop can be used to change the weight of the text. The `weight` prop should be a member of the `BoFontWeight` enum.

- `light`
- `regular`
- `medium`
- `semibold`
- `bold`

<Canvas of={BoTextStories.Weights} />

## Font family

The `fontFamily` prop can be used to change the font family of the text. The `fontFamily` prop should be a member of the `BoFontFamily` enum.

- `inter`
- `graphik`
- `sans`
- `mono`
- `serif`

<Canvas of={BoTextStories.FontFamilies} />

# White space

The `whiteSpace` prop can be used to change the white space of the text. The `whiteSpace` prop should be a member of the `BoTextWhiteSpace` enum.

- `normal`
- `nowrap`
- `pre`
- `pre-line`
- `pre-wrap`
- `break-spaces`

<Canvas of={BoTextStories.WhiteSpace} />
