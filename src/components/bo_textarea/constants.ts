export enum BoTextareaSize {
	small = 'small',
	default = 'default',
	large = 'large',
}

export enum BoTextareaState {
	none = 'none',
	valid = 'valid',
	invalid = 'invalid',
}

export enum BoTextareaVariant {
	default = 'default',
	filled = 'filled',
}
