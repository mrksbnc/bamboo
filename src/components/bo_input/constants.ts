export enum BoInputSize {
	small = 'small',
	default = 'default',
	large = 'large',
}

export enum BoInputState {
	none = 'none',
	valid = 'valid',
	invalid = 'invalid',
}

export enum BoInputType {
	text = 'text',
	password = 'password',
	email = 'email',
	number = 'number',
	tel = 'tel',
	url = 'url',
	search = 'search',
	date = 'date',
}

export enum BoInputVariant {
	default = 'default',
	filled = 'filled',
	underline = 'underline',
}
